<div id="<%= dom_id post %>">
  <h2><%= post.title %> </h2>
  <small>posted by <%= link_to post.user.name, user_path(post.user) %></small>
  <h6><%= pluralize(post.views, "view") %></h6>

  <p>
    <%= post.body %>
  </p>

</div>
